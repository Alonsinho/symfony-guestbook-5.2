# README

This README would normally document whatever steps are necessary to get the
application up and running.

Tests commands:

symfony console --env=test doctrine:fixtures:load -n --group=TestFixtures
symfony php bin/phpunit
